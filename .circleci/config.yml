# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

orbs:
  sonarqube: clicklogiq/sonarqube@1.0.1


workflows:
  sonarqube-scanner:
    jobs:
      - sonarqube/analyze
          context: 
            - sonar_aws_cybage
          #host_url: $SONAR_HOST_URL
          #login: $SONAR_TOKEN
          #projectKey: $CIRCLE_PROJECT_REPONAME
